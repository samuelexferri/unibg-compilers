#include <stdio.h>
#include <stdlib.h>

// Commento

/*
    Commento
*/

int a;
float b = 2.2;
char c;
int a = 1 // Errore catturato
int d = 1, e, f = 2;
a;
a = 2;
float g = (5+b)*4-5*3;
g = (5+b)*4;
//a = 1, b = 3;

int vettore1[] = {2, 3};
char vettore3[];

int *p1;
int *p2 = &a;
*p1 = &a;
*p1 = 5;

int vettore5[] = {5,6};
*vettore5 = 5;

char ca = 'a';

char special1 = '\n';
char special2 = '\0';

int sqrt(int a) {
    return 1;
}

void func_void();

int main (int j, char k) {
    int radice = sqrt(a)+2;

    int a;
    float b = 2.2;
    char c;
    //int a = 1; // Errore catturato
    int d = 1, e, f = 2;
    a;
    a = 2;
    float g = (5+b)*4-5*3;
    g = (5+b)*4;
    //a = 1, b = 3;

    int vettore1[] = {2, 3};
    char vettore3[];

    int *p1;
    int *p2 = &a;
    *p1 = &a;
    *p1 = 5;

    vettore5[] = {5,6};
    *vettore5 = 5;

    char ca = 'a';
    char *cb = &ca;

    func_void(t);
    func_void();

    //void notexist(); // Errore catturato
    //int notexist(); // Errore catturato

    return p1;
    //return *p1;
    //return &p1;
    //return funzione();
}

int statements(int t) {
		if (a == 5) {
			a;
		} else if (a == 5) {
			b;
			if (a == 5) {
			    a;
			}
		}  else {
		    c;
		}

        if (a == 5) {
            a;
        } else if (a == 5) {
            b;
        } else while (a > 0) {
            c;
        }

		while(a > 0) {
        	a = a - 1;
        }

        for(int b = a + 1; b <= (a + 5); b += 1) {
           a;
        }

        printf("a", "b=2", a, "Text for + * #", *b);
}