#include <stdio.h>
#include <stdlib.h>

int a = 2;
int b = 3;
//a = 1;

int main() {
    //a = 2 + 3 + 4 + 5 + 6 + 7;
    //a = 2 - 3 - 4 - 5 - 6 - 7;
    //a = 3 * 4 * 5;
    //a = +3 * 4 * 5; // Errore
    //a = 3 / 4 / 5;
    //a = 2 + 3 * 4 + 5;

    //a = (2+3)*4; // TODO Parentesi
    //a = 1 - 2 * 4 -  3 * 4 - 2; // TODO
    //a = 1 * 2 - 4 ;
    //a = 1 - 2 / 4 - 3/5; // TODO

    //int b = 3;

    //a = a / 5 + 3 ;
    //a = 5 / a + 3 ; // TODO
    return 0;
}

void func1() {
    int b = 3;

    if (b == 2) {
       int e = 7;
    } else if (b == 7) {
        int f = 8;
    } else {
        int g = 10;
    }
}

void func2() {
    int b = 3;

    while (b < 5) {
        int c = 3;
    }
}

void func3() {
    int b = 3;

    for (b = 2;b < 5; b += 5) {
        int c = 3;
    }
}