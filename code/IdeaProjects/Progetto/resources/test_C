#include <stdio.h>

float miao;
int ciao=2;
int vettore[4];
char vettore [];
char vettore []= { c, 5.4 , j, 4};
float vector [4]= {5,6};
int a = (5+b)*4;
void gino();
int a,b=220,c;

int vector[] = {'a','a'+c,3*e};
int vector[] = 'a'+c;
int vector[] = c;

vector[3] = 4;
int a = vector[3] + 'a';

int *a;
int *a = &b; // Verifica in semantica
*a = &b; // Controllare prima che è stato assegnato l'indirizzo per reference


int a = 3;

int a; // Definirla una sola volta
a = 3;

int a;
*a = 5;

vectort[] = 5;
*vectort = 5;

a = &b;

int vector[];
int *vector;

int miao (int t){
    int c +=3;
    k-=9;
    char a = a;
    char a = 'a';
    int a = (5+b)*4;
    int vettore[5];
    vettore[2]=2;
    kkk[]={1,2,r};

    int a,b=3,c = 3;

    int *a;
    int *a = &b; // Verifica in semantica

    *a = 5;

    int a = 3;

    int a; // Definirla una sola volta
    a = 3;

    int a;
    *a = 5;

     a = &b;

     a = &b;
}

int miao (int cosa, float bau){
    float d;
    f*=2; // Si confonde con i puntatori
    return 2; // Non accettare return in posizione impropria
    int pippo (); // Non accettare
    function( miao);
    gino();
    return miao;
    return p;
    return *p;
    return &p;
}

int function();

int function() {
		if(a == 5) { // TODO Gestire one line senza parentesi
			jhg;
		}
		else {
			ghfj;
		}


		while(a > 0) {
        			a = a - 1;
        		}

        for(int b = a + 1; b <= (a + 5); b += 1) {
           f;
        }

        printf("a", "b=2", a, "as ss for + * #", b);
}



/*
int function() {
		if((a == 5) && (b == 5)) {
			jhg;
		}
		else {
			ghfj;
		}
}*/
//ALTRE CASISTICHE
/*
int main() {
   int vettore[4];

   vettore[0] = 1;
   vettore[1] = 2;
   vettore[2] = 0;
   vettore[3] = -2;
}

int main() {
    int vettore[] = {1,2,3,4};  //inizializzazione e dichiarazione
}

int matrice[4][3] ={ {2,1,1}, {3,3,3}, {0,2,0}, {0,1,1} };


int main(void) {
    int *pa, a=10;

        pa=&a;
    printf("Indirizzo della variabile: %x ", pa);
    printf("Valore variabile: %d ", *pa);
//oppure avrei potuto scrivere: printf("Valore variabile: %d ", a);
    return 0;
}
*/