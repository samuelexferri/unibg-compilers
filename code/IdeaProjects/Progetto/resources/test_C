#include <stdio.h>
#include <stdlib.h>

// Commento

/*
    Commento
*/

int a;
float b;
char c;
int a = 1; // Semantica: definirla una sola volta
int a = 1, b, c = 2;
a;
a = 2;
a = b;
int a = (5+b)*4;
a = (5+b)*4;
a = 1, b = 3;

int vettore[5];
float vettore[4] = {5, 6};
char vettore [];
char vettore [] = {c, 5.4 , 3*b, 4}; // Semantica: non dovrebbe accettarlo

int *a;
int *a = &b; // Semantica: verifica indirizzo
*a = &b; // Semantica: controllare prima che è stato assegnato l'indirizzo per reference
*a = 5;
vettore[] = 5;
*vettore = 5;

void gino();

int main (int j, char k){
    int a;
    float b;
    char c;
    int a = 1; // Semantica: definirla una sola volta
    int a = 1, b, c = 2;
    a;
    a = 2;
    a = b;
    int a = (5+b)*4;
    a = (5+b)*4;
    a = 1, b = 3;

    int vettore[5];
    float vettore[4] = {5, 6};
    char vettore [];
    char vettore [] = {c, 5.4 , 3*b, 4}; // Semantica: non dovrebbe accettarlo

    int *a;
    int *a = &b; // Semantica: verifica indirizzo
    *a = &b; // Semantica: controllare prima che è stato assegnato l'indirizzo per reference
    *a = 5;
    vettore[] = 5;
    *vettore = 5;

    funzione(t);
    funzione();

    // void pippo(); // Ok, non lo accetta
    int pippo(); // TODO:: Non dovrebbe accettarlo

    return p;
    return *p;
    return &p;
    // return funzione(t); // TODO: Dovrebbe accettarla
}

int funzione();

int funzione(int t) {
		if (a == 5) {
			a;
		} else if (a == 5) {
			b;
			if (a == 5) {
			    a;
			}
		}  else {
		    c;
		}

        if (a == 5) {
            a;
        } else if (a == 5) {
            b;
        } else while (a > 0) {
            c;
        }

		while(a > 0) {
        	a = a - 1;
        }

        for(int b = a + 1; b <= (a + 5); b += 1) {
           a;
        }

        printf("a", "b=2", a, "Text for + * #", *b);
}